Language:
  type: object
  inherits: [Node]
  config:
    interfaces: [Node]
    description: A programming language where a project could have been written
    fields:
      name:
        type: String!
        description: The name of the programming language